from ypl.db import attachments, chats, language_models, payments, point_transactions, ratings, rewards, users

all_models = [attachments, chats, language_models, payments, point_transactions, ratings, rewards, users]
