name: Post Analytics

on:
  schedule:
    - cron: '2 8 * * *'  # 12:02 AM PST (8:02 UTC)
  workflow_dispatch:

jobs:
  post_analytics_production:
    uses: ./.github/workflows/cronjob.yml
    with:
      command: post-analytics
      environment: production
    secrets: inherit 
