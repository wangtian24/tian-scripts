name: Validate Pending Cashouts

on:
  schedule:
    - cron: '0 0 * * *'  # Runs every 24 hours at midnight UTC
  workflow_dispatch:

jobs:
  validate_pending_cashouts_production:
    uses: ./.github/workflows/cronjob.yml
    with:
      command: validate-pending-cashouts
      environment: production
    secrets: inherit 